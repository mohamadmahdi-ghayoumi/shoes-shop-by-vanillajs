import { El } from "../utils/create-element";

export function home() {
  // inputPassword.addEventListener()
  return El({
    element: "a",
    className: "",
    href: "/login",
    innerText: "gnfgn",
  });
}
